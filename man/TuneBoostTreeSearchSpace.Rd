% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/TuneBoostTreeSearchSpace.R
\name{TuneBoostTreeSearchSpace}
\alias{TuneBoostTreeSearchSpace}
\title{Montar espaço de busca Bayesiano}
\usage{TuneBoostTreeSearchSpace(learn_rate = c(0.01, 0.2), tree_depth = c(2L, 12L), min_n = c(1, 80), loss_reduction = c(0, 8), sample_size = c(0.55, 1), mtry = NULL, max_bin = NULL, lambda = NULL, alpha = NULL, max_delta_step = NULL, colsample_bytree = NULL, colsample_bylevel = NULL, num_leaves = NULL, min_data_in_leaf = NULL, scale_pos_weight = NULL)}
\arguments{\item{learn_rate}{Vetor numérico de tamanho 2 em \code{(0, 1]}. Limites da taxa de aprendizado.}\item{tree_depth}{Vetor numérico/inteiro de tamanho 2. Limites da profundidade máxima; valores são arredondados antes do fit.}\item{min_n}{Vetor numérico de tamanho 2. Limites do tamanho/peso mínimo de nó/folha.}\item{loss_reduction}{Vetor numérico de tamanho 2, não negativo. Limites do ganho mínimo exigido para split.}\item{sample_size}{Vetor numérico de tamanho 2 em \code{(0, 1]}. Limites da fração de linhas por iteração.}\item{mtry}{\code{NULL} ou vetor numérico de tamanho 2 em \code{(0, 1]}. Limites da fração de features por split/nó.}\item{max_bin}{\code{NULL} ou vetor numérico/inteiro de tamanho 2. Limites do número de bins de histograma.}\item{lambda}{\code{NULL} ou vetor numérico não negativo de tamanho 2. Limites de regularização L2.}\item{alpha}{\code{NULL} ou vetor numérico não negativo de tamanho 2. Limites de regularização L1.}\item{max_delta_step}{\code{NULL} ou vetor numérico não negativo de tamanho 2. Limites do estabilizador logístico do XGBoost; ignorado pelo LightGBM.}\item{colsample_bytree}{\code{NULL} ou vetor numérico de tamanho 2 em \code{(0, 1]}. Amostragem de features por árvore.}\item{colsample_bylevel}{\code{NULL} ou vetor numérico de tamanho 2 em \code{(0, 1]}. Amostragem de features por nível no XGBoost; ignorada pelo LightGBM.}\item{num_leaves}{\code{NULL} ou vetor numérico/inteiro de tamanho 2. Número máximo de folhas no LightGBM; ignorado pelo XGBoost.}\item{min_data_in_leaf}{\code{NULL} ou vetor numérico/inteiro de tamanho 2. Mínimo de linhas por folha no LightGBM; ignorado pelo XGBoost.}\item{scale_pos_weight}{\code{NULL} ou vetor numérico positivo de tamanho 2. Limites do peso da classe positiva.}}
\value{Lista validada com classe \code{tbtb_search_space}. Cada elemento é um vetor \code{c(lower, upper)} finito e crescente; elementos \code{NULL} são removidos e não são otimizados.}
\description{Define os limites inferiores e superiores dos hiperparâmetros que variam durante a otimização Bayesiana de \code{TuneBoostTree()}.}
\details{Parâmetros com o mesmo nome fixados em \code{TuneBoostTreeBoostParams()} prevalecem sobre este espaço. Frações devem estar em \code{(0, 1]}. Parâmetros inteiros, como \code{tree_depth}, \code{max_bin}, \code{num_leaves} e \code{min_data_in_leaf}, são amostrados em escala numérica e arredondados antes do ajuste.}
